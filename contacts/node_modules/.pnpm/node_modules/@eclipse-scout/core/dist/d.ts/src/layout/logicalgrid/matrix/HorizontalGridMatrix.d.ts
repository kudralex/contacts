import { GridData, LogicalGridMatrix, LogicalGridWidget } from '../../../index';
export declare class HorizontalGridMatrix extends LogicalGridMatrix {
    rowCount: number;
    constructor(columnCount: number);
    computeGridData(widgets: LogicalGridWidget[]): boolean;
    protected _add(widget: LogicalGridWidget, hints: GridData, data: GridData): void;
}
//# sourceMappingURL=HorizontalGridMatrix.d.ts.map